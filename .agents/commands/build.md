# Build Project

Build the workspace and report any compilation errors.

## Steps

1. Run `mise run build` for a development build
2. If there are compilation errors, analyze and fix them
3. Optionally run `mise run build-release` for a release build

## Command

```bash
mise run build
```

## Release Build

```bash
mise run build-release
